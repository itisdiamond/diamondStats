% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/function_diamond.R
\name{correlation_diamond}
\alias{correlation_diamond}
\title{Correlation Analysis with Confidence Intervals and Interpretation}
\usage{
correlation_diamond(
  x,
  y = NULL,
  data = NULL,
  subset = NULL,
  method = c("pearson", "spearman", "kendall"),
  method.subset = NULL,
  use = "complete.obs",
  digits = 4,
  conf.level = 0.95
)
}
\arguments{
\item{x}{A numeric vector or a formula of the form \code{y ~ x}.}

\item{y}{A numeric vector. Not required if \code{x} is a formula.}

\item{data}{Optional data frame in which variables in \code{x}, \code{y}, and \code{subset} are evaluated.}

\item{subset}{Optional vector (logical, factor, character, or numeric) used to define subgroups.}

\item{method}{Character vector specifying one or more correlation methods to use:
\code{"pearson"}, \code{"spearman"}, \code{"kendall"}. Defaults to all three.}

\item{method.subset}{Optional vector of correlation methods to apply for each subgroup (recycled if needed).}

\item{use}{String indicating how missing values are handled. Default is \code{"complete.obs"}.}

\item{digits}{Integer. Number of digits for rounding results (default is 4).}

\item{conf.level}{Confidence level for the interval (default is 0.95).}
}
\value{
A data frame with columns:
\describe{
\item{method}{The correlation method used.}
\item{coefficient}{The correlation coefficient.}
\item{shared_variance}{Squared coefficient (R²).}
\item{p_value}{P-value of the test.}
\item{ci_lower}{Lower bound of the confidence interval.}
\item{ci_upper}{Upper bound of the confidence interval.}
\item{interpretation}{Qualitative interpretation (e.g., "Moderate", "Strong").}
\item{n}{Sample size.}
\item{group}{Group name (if subset by group).}
}
}
\description{
Computes Pearson, Spearman, and/or Kendall correlation between two numeric vectors,
optionally grouped by a subset factor. Outputs coefficient, shared variance, p-value, confidence intervals,
and a qualitative interpretation of strength.
}
\examples{
correlation_diamond(mtcars$mpg, mtcars$hp)
correlation_diamond(mpg ~ hp, data = mtcars)
correlation_diamond(mpg ~ hp, data = mtcars, subset = mtcars$cyl)

}
